# 정신과 의사의 모험 — JMS · 게임 분석 문서

> 분석 대상: `jms_game.html` (846KB, 단일 HTML)
> 엔진: PixiJS 7.4.2 (인라인) / WebGL
> 게임 코드 영역: 1138–1885행 (그 앞 27–1137행은 인라인 PixiJS 라이브러리)

---

## 1. 한눈에 보기

- **장르**: *Ace Attorney(역전재판)* 메커닉을 정신과 진료로 옮긴 **진료 추리 어드벤처**
- **컨셉**: 신입 정신과 의사가 환자의 말에서 단서를 모아 → 백과로 병명·처방을 확인 → 환자기록부를 작성 → 진단 제출
- **배경**: 제일종합병원 정신건강의학과, 2018년 3월 8일
- **플레이타임**: 약 3~5분 (단일 케이스, 분기 없는 직선형)
- **특이점**: 원작 영상을 PixiJS로 "1:1 재현"한 작품. 캐릭터는 외부 이미지, UI·이펙트·사운드는 전부 코드로 생성

---

## 2. 등장인물

| 역할 | 이름 | 설정 |
|------|------|------|
| 주인공(플레이어) | **이현종** | 첫 출근한 정신과 의사 (남) |
| 보조/튜토리얼 | **한석지** | 간호사. 친근한 어조로 시스템 설명, 오진 시 피드백 |
| 환자 (CASE 1) | **이승헌** | 남 27세, 고시생 겸 편의점 아르바이트. 주호소 "우울" |

---

## 3. 게임 흐름 (상태 머신)

```
boot → title → intro → consult → verdict → end
```

1. **boot()** (1331행): 폰트·텍스처 로드 → 9개 레이어 생성 → 배경/HUD 빌드
2. **title** (showTitle, 1513행): 타이틀 + "진료 시작하기" 버튼 대기
3. **intro** (startIntro, 1524행): 간호사 한석지의 튜토리얼 대사 4개
4. **consult** (startConsult, 1631행): **메인 게임플레이** — 환자 진술 청취 → 단서 수집 → 기록부 작성 → 제출
5. **verdict** (approve, 1828행): 배지 컷씬 → APPROVED 스탬프 → 결과 팝업
6. **end** (ending, 1877행): "CASE 1 CLEAR" 엔딩, 후속 예약 안내

전역 상태는 `G` 객체(1220행)로 일관 관리: `G.state`, `G.tween[]`, `G.dragging`, `G.clueTokens[]`, `G.unlocked{disease,drug}` 등.

---

## 4. 핵심 게임플레이 메커닉

### (1) 진술 청취 → 단서 확보
- 환자 대사 5개를 `◀ ▶`로 순회 (1/5 ~ 5/5)
- 증상 대사에서 "🔍 단서 확보" 버튼 탭 → 단서 토큰 생성 (중복 방지)
- 수집되는 단서 4종: **흥미·의욕 저하 / 지속적인 슬픔 / 불면·식욕 저하 / 활력 부족**
- 5번째 대사("죽고 싶다는 생각은 안 해요")는 단서가 아니라 **심각도 판정용 함정** — 자살 암시 없음 = 초기

### (2) 의료지식백과 — 핵심 언락 장치
- 증상 토큰을 백과의 책 아이콘에 **드래그&드롭** → `lookupSymptom()`
- 한 번의 정확한 조회로 **「우울증(질병)」 토큰 + 「파란약」 토큰을 동시 언락**
- 백과 본문에 심각도별 처방 기준 표시 (초기 = 파란약 ● / 심각 = 파란약 ●●)

### (3) 드래그&드롭 토큰 시스템
| 토큰 | 색 | 획득처 |
|------|----|--------|
| 🗨 증상 | 청록 | 환자 대사 |
| 🩺 질병 | 보라 | 백과 언락 |
| 💊 약물 | 파랑 | 백과 언락 |

토큰을 환자기록부의 슬롯(치료/진단/처방)에 드롭하여 진단서를 완성.

### (4) 승리 조건 (4개 동시 만족)
1. 증상 단서 **≥ 3개**
2. 진단명에 **"우울증"** 포함
3. 심각도 **"초기"** 선택
4. 처방에 **"파란약"** 포함

→ 만족 시 `approve()`, 하나라도 미달이면 `reject(msg)` (오진 피드백 후 재시도, 무한)

### (5) 오진 피드백 (역전재판 오마주)
- 사운드 `Audio2.buzz()` + 폴더 흔들기 `shakeFolder()` + 빨간 "오진!" 플래시
- 항목별 안내 메시지 (단서 부족 / 진단명 오류 / 심각도 미체크 / 심각도 오류 / 처방 오류)

---

## 5. 기술 구현

### 렌더링
- **캐릭터/배경**: Base64 PNG 스프라이트 (ASSETS 객체, `nurse_calm`/`nurse_smile`/`patient`)
- **UI 전체**: PixiJS Graphics로 프로시저럴 생성 (`rrect()`, `strokeRR()`, `button()`)
- **배경 그래디언트**: Canvas 2D → Texture 변환 (`gradTexture()`)

### 애니메이션
- 자체 트윈 엔진(`tween()`, 1251행) + `app.ticker`의 `tickTweens()`
- 이징: Ease.out / in / outBack(오버슈트) / outElastic(탄성)
- 대화 타이핑(26ms/글자), 프롬프트 깜박임(사인파 알파), 플래시 텍스트, 배지 컷씬, APPROVED 스탬프 회전

### 사운드 — Web Audio API 절차적 합성 (저작권 회피)
- `Audio2` IIFE(1181행)가 Oscillator/Noise로 실시간 SFX 생성
- blip(타이핑) / ding(성공) / pick(클릭) / drop(배치) / buzz(오류) / stamp / page / jingle(엔딩) / sad
- 마스터 게인 0.32, 음소거 토글 지원

### 폰트
- Google Fonts: Gothic A1 / Jua / Do Hyeon / Black Han Sans (한글)
- CSS Font Loading API로 사전 로드 후 게임 시작

### 해상도/입력
- 가상 해상도 1024×728, `fitCanvas()`로 윈도우에 맞춰 스케일, DPR 최대 2배
- 순수 포인터 기반: `pointermove/up/tap` + 드래그&드롭 (키보드 입력 없음, 터치 호환)

---

## 6. 테마·메시지

- **게임화된 진료 프로세스**: "환자기록부", "단서", "의료지식백과" 등 실제 정신과 진료 흐름을 추리 게임 메커닉으로 번역
- **역전재판 패러디**: "오진!" + 폴더 흔들기 연출은 "이의 있습니다!"의 오마주
- **교육적 설계**: 증상을 체계적으로 수집하고 심각도(초기 vs 심각)를 구분해야만 정답 — 정신과 진단에서 중증도 평가의 중요성을 반영
- **시리즈화 암시**: 엔딩에서 "2018년 3월 14일, 새로운 증상으로 진료 예약" → CASE 2 확장 여지

---

## 7. 현재 한계 / 확장 포인트

- **콘텐츠가 CASE 1 하나뿐** — 백과·검증 로직은 우울증/파란약에 하드코딩되어 있어 케이스 추가 시 데이터 구조 일반화 필요
- 분기 없는 직선형 — 다중 진단/오답 선택지 추가 여지
- 단서 트레이/슬롯 레이아웃은 고정 폭 기반이라 케이스 데이터가 커지면 조정 필요
